# ⚠️ DEPRECATED: 此文件已弃用
#
# 测试环境请使用 docker-compose.prod.yml 配合环境变量
# 或者直接在 CI 中使用 docker-compose.yml (默认配置)
#
# 保留此文件仅为向后兼容，将在未来版本中移除
# 最后更新: 2026-03-13

# 测试环境覆盖配置
# 使用: docker compose -f docker-compose.yml -f docker-compose.test.yml up
#
# 特性:
# - 隔离的数据库
# - 快速失败的健康检查
# - 中等资源限制

services:
  postgres:
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-qilema_test}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-test_password}
      POSTGRES_DB: ${POSTGRES_DB:-qilema_test}
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  redis:
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 256M

  backend:
    environment:
      ENVIRONMENT: testing
      DEBUG: "True"
      LOG_LEVEL: INFO
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    healthcheck:
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 5s

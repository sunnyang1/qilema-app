apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: qilema
  labels:
    app: nginx
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: nginx

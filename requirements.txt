# 起了吗 App - 后端依赖
# 实际依赖定义在 backend/requirements.txt
# 此文件保留用于兼容性

# 安装命令：
#   pip install -r backend/requirements.txt
#
# 或使用 Docker：
#   docker-compose up -d

# 如需从根目录运行，请执行：
#   cd backend && pip install -r requirements.txt && uvicorn main:app --reload
